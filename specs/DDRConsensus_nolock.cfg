\* Same model, lock DISABLED. TLC must find a counterexample to Agreement,
\* demonstrating the lock rule is load-bearing (TLA+ analogue of the Rust
\* `lock_is_load_bearing` test).
CONSTANTS
  Validators = {v1, v2, v3, v4}
  Faulty = {v4}
  Values = {a, b}
  MaxRound = 3
  q = 3
  UseLock = FALSE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT Agreement
